# dsh bundle patch: activate the dsh-wordgap plugin in the profile config.
# Format per dsh docs (dsh.bundle.patch); verify against a real install --
# reference implementations: dsh-web-attention-badge / turtle-ui.
plugins:
  dsh-wordgap: {}
